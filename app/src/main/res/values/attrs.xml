<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>


    <!-- Declare custom attribues for the SetGameCard object -->
    <declare-styleable name="SetGameCardView">

        <!-- SetGame attributes should accessible -->
        <attr name="symbol_shape" format="enum">
            <enum name="oval" value="0" />
            <enum name="diamond" value="1" />
            <enum name="squiggle" value="2" />
        </attr>
        <attr name="symbol_color" format="enum">
            <enum name="red" value="0" />
            <enum name="green" value="1" />
            <enum name="purple" value="2" />
        </attr>
        <attr name="symbol_count" format="enum">
            <enum name="one" value="0" />
            <enum name="two" value="1" />
            <enum name="three" value="2" />
        </attr>
        <attr name="symbol_fill" format="enum">
            <enum name="solid" value="0" />
            <enum name="open" value="1" />
            <enum name="stripe" value="2" />
        </attr>

        <!-- Allow custom width/height to be set with aspect maintained -->
        <attr name="aspectWidth" format="integer" />
        <attr name="aspectHeight" format="integer" />

    </declare-styleable>

</resources>
